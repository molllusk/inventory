<div id="filterrific_results">

  <div class='center' style='margin-bottom: 30px;'>
    <%= page_entries_info products %>
  </div>
  
  <table class="products full_width">
    <tr>
      <th>Added (Pacific Time)</th>
      <th>Product (vend name)</th>
      <th>SKU</th>
      <th>Vend SF Qty</th>
      <th>Shopify SF Qty</th>
      <th>3rdParty?</th>
      <th>Sale?</th>
      <th>Vend</th>
      <th>Shopify</th>
    </tr>
    <% products.each do |product| %>
      <% vend = product.vend_datum %>
      <% shopify = product.shopify_datum %>
      <tr>
        <td><%= product.created_at.in_time_zone("Pacific Time (US & Canada)").to_s(:humanized_ago) %></td>
        <td><%= link_to vend.name, product %></td>
        <td><%= vend.sku %></td>
        <td><%= vend.inventory || 'missing' %></td>
        <td>
          <% if shopify.present? %>
            <%= shopify.inventory || 'missing' %>
          <% else %>
            n/a
          <% end %>
        </td>
        <td>
          <% if shopify.present? %>
            <%= product.third_party? %>
          <% else %>
            n/a
          <% end %>
        </td>
        <td>
          <% if shopify.present? %>
            <%= product.sale? %>
          <% else %>
            n/a
          <% end %>
        </td>
        <td><a href='https://mollusksurf.vendhq.com/product/<%= vend.vend_id %>' target='_blank'>Vend page</a></td>
        <td>
          <% if shopify.present? %>
            <a href='https://mollusksurf.myshopify.com/admin/products/<%= shopify.shopify_product_id %>/variants/<%= shopify.variant_id %>' target='_blank'>Shopify page</a>
          <% else %>
            n/a
          <% end %>
        </td>
      </tr>
    <% end %>
  </table>

  <div class='center'>
  <%= will_paginate %>
  </div>
</div>
